package forth.commands;

import forth.ExecutionContext;
import forth.Command;

public class ThenCommand implements Command {
    @Override
    public void execute(ExecutionContext context) {
        //just ends block
    }
}
