: fibonacci ( n -- )
  dup 0= if drop else
  dup 1 = if drop 0 . else
  dup 2 = if drop 0 . 1 . else
  0 . 1 .
  0 1
  rot 2 - 0 ?do
    over over +
    dup .
    swap rot drop
  loop
  2drop then then then ;